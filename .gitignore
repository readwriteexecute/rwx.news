# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal
/db/sphinx

# Ignore all logfiles and tempfiles.
/log
/tmp

vendor/bundle
public/assets

# files added in production
lib/tasks/deploy.rake
config/initializers/production.rb
config/database.yml
config/initializers/secret_token.rb
config/*.sphinx.conf
config/unicorn.conf.rb

# rwx.news SMTP creds stored here
config/initializers/email.rb

# rwx.news console helper script w/ correct env vars
script/console

# rwx.news entrypoint that systemd -> unicorn is wired up to w/ correct env
# vars and unicorn invocation
.unicorn.sh
